apiVersion: 1

providers:
  - name: borgscale
    orgId: 1
    folder: BorgScale
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
